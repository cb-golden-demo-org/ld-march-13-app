apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer
on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:
  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['ld-march-13-api']['hackers-api'].deploy }}
    uses: cb-golden-demo-org/ld-march-13-api/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['ld-march-13-api']['hackers-api'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['ld-march-13-api']['hackers-api'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['ld-march-13-auth']['hackers-auth'].deploy }}
    uses: cb-golden-demo-org/ld-march-13-auth/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['ld-march-13-auth']['hackers-auth'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['ld-march-13-auth']['hackers-auth'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['ld-march-13-web']['hackers-web'].deploy }}
    uses: cb-golden-demo-org/ld-march-13-web/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['ld-march-13-web']['hackers-web'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['ld-march-13-web']['hackers-web'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
